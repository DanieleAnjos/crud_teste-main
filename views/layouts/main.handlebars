<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">    
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f7fa;
            color: #333;
        }

        header {
            background: linear-gradient(90deg, #007bff, #6c63ff);
            color: white;
            padding: 20px 0;
            text-align: center;
        }

        .navbar-brand {
            font-size: 1.8rem;
            font-weight: bold;
            text-transform: uppercase;
        }

        .nav-link {
            font-size: 1.1rem;
            margin-right: 20px;
            transition: color 0.3s ease;
            color: #fff !important;
        }

        .nav-link:hover {
            color: #ffc107 !important;
        }

        main {
            padding: 40px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }

        footer {
            text-align: center;
            padding: 15px 0;
            background-color: #343a40;
            color: #ffffff;
            font-size: 0.9rem;
            position: fixed;
            bottom: 0;
            width: 100%;
            box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1);
        }

        button, .nav-link {
            transition: all 0.3s ease;
        }

        button:hover, .nav-link:hover {
            transform: translateY(-3px);
        }

        .navbar-brand i {
            font-size: 1.5rem;
            margin-right: 8px;
        }

        i.fas {
            color: white;
            transition: transform 0.3s ease;
        }

        i.fas:hover {
            transform: rotate(360deg);
        }

        .container {
            max-width: 960px;
        }

        .btn-custom {
            background-color: #6c63ff; 
            color: white;
            padding: 10px 20px;
            border-radius: 30px;
            border: none;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #5652d9; 
        }

        /* Estilos da tabela */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd; 
        }

        th {
            background-color: #6c63ff; 
            color: #f3f0f0; 
        }

        .navbar-nav .nav-item {
            margin-right: 20px;
        }

        .navbar-nav .nav-item:last-child {
            margin-right: 0;
        }

        @media (max-width: 768px) {
            main {
                margin-top: 20px;
                padding: 20px;
            }
            .navbar-brand {
                font-size: 1.5rem;
            }
        }

        a.nav-link:hover {
            background-color: rgba(124, 207, 189, 0.596);
            border-radius: 8px;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
<header>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fas fa-book"></i> Biblioteca
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/books/create"><i class="fas fa-plus"></i> Adicionar Livro</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/books"><i class="fas fa-list"></i> Listar Livros</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/books/search"><i class="fas fa-search"></i> Buscar Livro</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>

    <!-- Main content -->
    <main class="container">
        {{{body}}}
    </main>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 Sistema de Biblioteca - Todos os direitos reservados</p>
    </footer>

    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
